Before the roadmap — two of your own rules are in tension, and I'd rather say so now than have it surface silently three phases in.
"Approximately one day per phase" and "fully tested, documented, production-ready, no placeholders" don't fit together for most of these phases. A Metadata Engine with the Question hierarchy (Single/Multi/Grid/Rank/Numeric/Text/Derived) plus validators, done to a genuine production bar with real unit tests, is several days of senior-engineer work on its own — Phase 5 (Rule Engine) with a restricted expression evaluator is security-sensitive code that needs adversarial test cases, not just happy-path coverage, and that alone eats a day before you've written a single built-in rule type. I'll size phases as logical units of fully-done work, not literal one-day boxes — if a phase would take a real team three days to do properly, I'll say so rather than ship something thinner to fit an artificial day-boundary and call it "production-ready."
One structural change to your example phase list, explained before I do it (per your own rule 9): I'm folding "Packaging" into Phase 1 rather than making it a late phase — a pyproject.toml/src-layout/CI skeleton that every later phase builds on should exist before Phase 2, not be bolted on at the end. And I'm not giving GUI a near-term phase number at all — the earlier architecture doc already deprioritized it as "future," and pretending it's phase-13-of-14 alongside core engine work overstates its priority.
Implementation Roadmap
#PhaseObjectiveKey deliverablesDepends onComplexityMain riskAcceptance criteria1FoundationProject skeleton everything else builds onpyproject.toml, src layout, core exceptions, core enums/types, config loader, logging setup, plugin registry primitive—MediumGetting the exception hierarchy/plugin contract wrong here means rework laterpip install -e . works, pytest green, importable package2Core Domain ModelsThe Codebook/Question hierarchyQuestion ABC + SingleQuestion/MultiQuestion/GridQuestion/RankingQuestion/NumericQuestion/TextQuestion/DerivedVariable, Category, Codebook container1Medium-HighGrid/derived modeling decisions are hard to change once parsers/rules depend on themFull type coverage, round-trip serialization test, circular-dependency detection for derived vars3Data Engine (pandas backend only)Abstract data access, one real backendDataBackend protocol, PandasBackend, CSV loader, format sniffing1MediumProtocol too pandas-shaped, hard to add DuckDB laterLoad CSV ? typed data handle, backend swappable in tests via a fake4Metadata Engine (native only)Parse/validate the native YAML codebook formatYamlMetadataParser, CodebookValidator (circular deps, orphan categories, id collisions)2MediumNone major — this is the best-specified phaseRound-trips a hand-written YAML codebook into Codebook and back5Rule EngineRestricted expression evaluator + rule DSLAST-based evaluator (no eval), rule dataclasses (RangeRule, RequiredRule, SkipLogicRule, SumEqualsRule, MaxSelectionsRule, ExclusiveCategoryRule, RankRule), YAML?rule parser2, 4HighSecurity surface — needs adversarial tests (attempted injection, attribute access, etc.), not just happy pathMalicious when: string is rejected, not executed; every built-in rule type has a passing + failing case6Validation ExecutionWire Rule Engine to real dataValidationContext, ErrorCollector, ValidationResult model, vectorized execution for patterns 1/2/3/6/7/8 from the rules KB2, 3, 5Medium-HighVectorizing skip-logic/rank patterns correctly across a full dataframe, not just per-rowRuns full rule set against a synthetic dataset, results match a hand-computed expected set7Cleaning EngineTransformations or the data itselfTransformer ABC, grid/multi expansion, recode, derived-variable builder (topological order from depends_on)2, 3Medium-HighDerived-variable ordering bugs are subtle and silentDerived variable with a 3-deep dependency chain computes correctly; circular dependency raises before running8Execution PipelineOrchestrate everything end-to-endOrchestrator, on_start/on_case/on_end hook registry, run manifest (resumability)3, 6, 7MediumManifest write atomicity (crash mid-write)Kill process mid-run, rerun resumes correctly from manifest state9Reporting EngineStructured results ? filesHTMLReport, ExcelReport, JSONReport, CSVReport, all consuming one shared result model6Low-MediumNone majorSame result set produces consistent counts across all four formats10Plugin System hardeningFormalize entry_points discovery across every extension point already sketched in Phase 1Real third-party-style test plugin package, discovery tests1–9Low-MediumShould be low-risk if Phase 1's registry primitive was designed rightAn out-of-tree pip-installed dummy plugin is discovered and used without core code changes11CLIsurvey init/validate/clean/export/reporttyper app wrapping the Orchestrator8, 9LowNone majorEach command runs end-to-end against a fixture project12Legacy migration bridge.sav import + shadow-run comparatorpyreadstat-based importer, legacy _ErrorLog.txt/_QAout2.txt parser, DivergenceReport2, 6HighUnverified dependency — flagged three turns ago and still not checked: whether pyreadstat preserves everything MDM.Fields reports (MRSETs, custom attributes). This phase should not start until that spike is run against a real production .sav.Diff of pyreadstat-derived codebook vs a real MDM.Fields dump, on an actual file, not a synthetic one13GUI (deferred)Thin PySide6 layerNot scoped in detail yet — deliberately, per the "future-ready" framing from the original architecture ask11UnscopedPremature to detail before the CLI/engine are stableN/A until prioritized
Phase 1 starts now. Setting up the actual project.Ran 2 commandsRan 2 
commands continue

Pivoted toward examining the types modulePivoted toward examining the types moduleNow the exception hierarchy:Create the core exception hierarchy for the whole frameworkCreate the core exception hierarchy for the whole frameworkNow the config module:Create the pydantic-based configuration model and loaderCreate the pydantic-based configuration model and loaderNow logging config:Architected extensible plugin registry with dynamic discovery mechanismsArchitected extensible plugin registry with dynamic discovery mechanismsNow the tests.Created 4 filesCreated 4 filesNow config tests:Investigated exception handling discrepancies between frozen modelsInvestigated exception handling discrepancies between frozen modelsNow the plugin registry tests:Create unit tests for the plugin registryCreate unit tests for the plugin registryNow let's run the full test suite.Run the Phase 1 test suiteRun the Phase 1 test suiteAll 38 tests pass. Let me also verify mypy-clean typing since strict typing was a stated requirement, and add a README.Ran 2 commandsRan 2 commandsClean strict mypy pass. Adding the README and a coverage check, then wrapping up.Ran 2 commands, viewed a fileRan 2 commands, viewed a fileThat's the unreadable-file (permission-denied) branch. Adding a test for it to close the coverage gap.Reconciled test coverage limitations and finalized project documentationReconciled test coverage limitations and finalized project documentationNow packaging everything for delivery and running the full suite one last time as a final sanity check.Ran 3 commandsRan 3 commandsClean zip, no cache artifacts.Presented filePresented filePhase 1 — Foundation: complete
What was implemented:

core/types.py — QuestionType, DataType, Severity enums (StrEnum, YAML-friendly)
core/exceptions.py — full exception hierarchy, including leaves for subsystems not built yet (each documented with which phase raises it, so this module won't need breaking changes later)
core/config.py — pydantic AppConfig/PathsConfig/LoggingConfig, YAML loader with eager validation
core/logging_config.py — stdlib dictConfig-based setup, console + optional file handler
plugins/registry.py — generic PluginRegistry[T], manual registration + entry_points-based discovery, no implicit import-time magic

Folder structure: as shown in the roadmap table; matches README.md's layout section exactly.
Design decisions worth flagging:

Exception hierarchy defined in full now, not incrementally — avoids restructuring imports every later phase.
AppConfig is frozen (immutable) — configuration shouldn't mutate mid-run.
Plugin registry discovery is explicit, never automatic on import — a deliberate choice to keep plugin behavior predictable and testable.

Tests: 38 passed, 1 skipped. The skip is real, not swept under the rug: a permission-denied test for load_config's file-read error path is skipped because this sandbox runs as root, which bypasses Unix file permissions — that test will actually execute (and should pass) in a normal CI environment or on your machine. Coverage is 99% (94% on config.py specifically, for that same skipped branch). mypy --strict passes clean on all 8 source files.
How to verify: unzip, then:
bashpip install -e ".[dev]"
pytest -v
mypy src/opensurvey --strict
Honesty about environment: this sandbox has Python 3.12.3, not 3.13. Nothing here uses a 3.13-exclusive feature, but I haven't verified that claim on an actual 3.13 interpreter — run the same commands on your machine before trusting this phase fully.
What Phase 2 should be: Core Domain Models — the Question hierarchy and Codebook, per the roadmap. It depends on nothing from this phase except the exception classes and QuestionType/DataType enums already built here.
Waiting for your review before starting Phase 2.
